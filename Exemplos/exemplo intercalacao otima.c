#include <stdio.h>
#include <stdlib.h>

/*
optimalMerge(v) {
	h = new HeapMin();
	h.Build(v);
	for(i=1; i<k; i++) {
		no = new TreeNode();
		no.esq = h.ExtractMin();
		no.dir = h.ExtractMin();
		no.valor = no.esq -> valor + no.dir -> valor;
		h.Insert(no);
	}
	return h.ExtractMin();
}
*/

int main(void) {
	return 0;
}
